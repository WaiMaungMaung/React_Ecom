import styled from "@emotion/styled";
import React from "react";
import { popularProducts } from "./data";

const Container = styled.div``;
const Product = (props) => {
  return <Container>props.id</Container>;
};

export default Product;
